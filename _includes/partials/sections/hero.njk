<section id="hero" class="hero section">
  <div class="container">
    <div class="row align-items-center g-5">

      <div class="col-lg-6 hero-content">
        <h1>{{ hero.headline }}</h1>
        <p class="lead">{{ hero.lede }}</p>

        <div class="stats-row d-flex flex-wrap gap-4 my-4">
          {% for stat in hero.stats %}
            <div class="stat-item">
              <span class="stat-number d-block">{{ stat.value }}</span>
              <span class="stat-label">{{ stat.label }}</span>
            </div>
          {% endfor %}
        </div>

        <div class="action-buttons d-flex flex-wrap gap-3">
          <a href="{{ hero.ctaPrimary.url | url }}" class="btn-primary">{{ hero.ctaPrimary.label }}</a>
          <a href="{{ hero.ctaSecondary.url | url }}" class="btn-secondary">{{ hero.ctaSecondary.label }}</a>
        </div>
      </div>

      <div class="col-lg-6 hero-media position-relative">
        <img src="{{ hero.image | url }}" alt="{{ hero.imageAlt }}" class="img-fluid main-image rounded">
        <div class="image-overlay">
          <div class="badge-accredited">
            <i class="bi {{ hero.badge.icon }}"></i>
            <span>{{ hero.badge.label }}</span>
          </div>
        </div>
      </div>

    </div>
  </div>
</section>
